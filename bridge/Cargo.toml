[package]
name = "gmod-mcp-bridge"
version = "0.1.0"
edition = "2021"

[lib]
name = "gmsv_gmod_mcp_bridge"
crate-type = ["cdylib"]

[dependencies]
gmod = { version = "17.0.1", default-features = false }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
